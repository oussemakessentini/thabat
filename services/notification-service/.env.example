# Local development for notification-service. Copy to .env — do not commit .env.

SERVER_PORT=8085
SPRING_PROFILES_ACTIVE=local

# Must match Identity Service JWT_SECRET (at least 32 characters).
# IDEA/OS env JWT_SECRET overrides this file — avoid a truncated value there.
JWT_SECRET=u5tsVmLELhVrrNRURFDMtkpNuyJs1tI9SHWXodvY948=

DB_URL=jdbc:postgresql://localhost:5433/thabat_notification_db
DB_USERNAME=postgres
DB_PASSWORD=

EUREKA_SERVER_URL=http://localhost:8761/eureka/

KAFKA_BOOTSTRAP_SERVERS=localhost:9092
